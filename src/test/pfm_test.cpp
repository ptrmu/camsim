
#include "catch2/catch.hpp"

#include "../pfm/pfm_run.hpp"

namespace camsim
{
#if 0
  TEST_CASE("pfm_test - 0")
  {
    REQUIRE(pfm_run(0) == 0);
  }

  TEST_CASE("pfm_test - 1")
  {
    REQUIRE(pfm_run(1) == 0);
  }

  TEST_CASE("pfm_test - pfm_simple_rotation_example()")
  {
    REQUIRE(pfm_simple_rotation_example() == 0);
  }

  TEST_CASE("pfm_test - pfm_optimize_pose3()")
  {
    REQUIRE(pfm_optimize_pose3() == 0);
  }
#endif
}
